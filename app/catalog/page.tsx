import Link from 'next/link';import {products} from '@/lib/data';export default function Catalog(){return <main className="section"><Link className="text-link" href="/">← Home</Link><div className="section-head"><p className="eyebrow">Shop / Project Cart</p><h1>Product & Plant Catalog</h1><p>Quote-cart model for plants, materials, drainage, lighting, and outdoor living products.</p></div><div className="grid four">{products.map(p=><article className="card catalog-card" key={p.id}><div className="visual placeholder-img">{p.category}</div><h3>{p.name}</h3><p>{p.description}</p>{p.styleTags.map(t=><span className="tag" key={t}>{t}</span>)}{p.sunTags?.map(t=><span className="tag" key={t}>{t}</span>)}<div className="button-row"><button className="btn secondary">Add to design</button><Link className="btn primary" href="/cart">Add to project</Link></div></article>)}</div></main>}
