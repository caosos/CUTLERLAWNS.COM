import Link from 'next/link';
import { products } from '@/lib/data';

export default function Catalog(){return <main className="section"><Link className="text-link" href="/">← Home</Link><div className="section-head"><p className="eyebrow">Shop / Project Cart</p><h1>Project catalog with real Cutler service visuals.</h1><p>Build a quote request from common landscape, cleanup, irrigation, and property-preservation services. Product-specific plant photography can replace these temporary catalog visuals later.</p></div><div className="grid four">{products.map(p=><article className="card catalog-card" key={p.id}><img src={p.image} alt={p.name}/><span className="tag">{p.category}</span><h3>{p.name}</h3><p>{p.description}</p>{p.styleTags.map(t=><span className="tag" key={t}>{t}</span>)}{p.sunTags?.map(t=><span className="tag" key={t}>{t}</span>)}<div className="button-row"><Link className="btn secondary" href="/design">Add to Project</Link><Link className="btn primary" href="/cart">Request Estimate</Link></div></article>)}</div></main>}
