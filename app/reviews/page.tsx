import Link from 'next/link';import {reviewSources} from '@/lib/data';export default function Reviews(){return <main className="section"><Link className="text-link" href="/">← Home</Link><div className="section-head"><p className="eyebrow">Review Sources</p><h1>Real source-linked trust content</h1><p>Do not create fake testimonials. Live feed ready when credentials are connected.</p></div><div className="grid three">{reviewSources.map(r=><article className="card"><h3>{r.title}</h3><a className="text-link" href={r.url}>Open review source</a></article>)}</div></main>}
