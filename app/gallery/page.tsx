import Link from 'next/link';
import { gallerySections, pamphlets } from '@/lib/data';

export default function Gallery(){return <main className="section"><Link className="text-link" href="/">← Home</Link><div className="section-head"><p className="eyebrow">Gallery / Stories / Commercial Proof</p><h1>Real Cutler work, organized by service need.</h1><p>Browse local project visuals for commercial maintenance, residential refreshes, irrigation, seasonal cleanup, winter response, and pressure washing.</p><div className="hero-actions"><Link className="btn primary" href="/estimate">Request an Estimate</Link><Link className="btn secondary" href="/design">Open Design Studio</Link></div></div>{gallerySections.map(section=><section className="gallery-section" key={section.title}><div className="section-head compact"><p className="eyebrow">{section.items.length} visual{section.items.length>1?'s':''}</p><h2>{section.title}</h2></div><div className="grid three">{section.items.map(g=><article className="card media-card" key={`${section.title}-${g.title}`}><img src={g.image} alt={g.title}/><span className="tag">{g.tag}</span><h3>{g.title}</h3><p>{g.caption}</p><div className="button-row"><Link className="btn secondary" href="/design">Use as inspiration</Link><Link className="btn primary" href="/cart">Request Estimate</Link></div></article>)}</div></section>)}<section className="gallery-section"><h2>Pamphlets</h2><div className="grid three">{pamphlets.map(p=><article className="card" key={p.title}><h3>{p.title}</h3><p>{p.description}</p><a className="text-link" href={p.file}>PDF location</a></article>)}</div></section></main>}
