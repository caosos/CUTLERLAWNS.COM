import Link from 'next/link';import {gallery,pamphlets} from '@/lib/data';export default function Gallery(){return <main className="section"><Link className="text-link" href="/">← Home</Link><div className="section-head"><p className="eyebrow">Gallery / Stories / Videos</p><h1>Visual content library</h1><p>Subsections include Featured Work, Before / After, Seasonal Refresh, Commercial Properties, Residential Transformations, Winter Response, Property Preservation, and Pressure Washing Results.</p></div><div className="grid three">{gallery.map(g=><article className="card"><div className="visual placeholder-img">{g.type}</div><h3>{g.title}</h3><span className="tag">{g.tag}</span><p>Replace placeholder with owned image/video thumbnail at {g.image}.</p></article>)}</div><h2>Pamphlets</h2><div className="grid three">{pamphlets.map(p=><article className="card"><h3>{p.title}</h3><p>{p.description}</p><a className="text-link" href={p.file}>PDF location</a></article>)}</div></main>}
